<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/track_info_bg"
    android:padding="10dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/imgSong"
        android:layout_width="60dp"
        android:layout_height="60dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:src="@drawable/adele" />

    <TextView
        android:id="@+id/txtnameSong"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Em không sai chúng ta sai"
        android:textColor="#fff"
        android:layout_marginLeft="10dp"
        app:layout_constraintTop_toTopOf="@id/imgSong"
        app:layout_constraintBottom_toTopOf="@id/txtnameSinger"
        app:layout_constraintLeft_toRightOf="@id/imgSong"/>

    <TextView
        android:id="@+id/txtnameSinger"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ERIK"
        android:textColor="#fff"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@+id/txtnameSong"
        app:layout_constraintLeft_toRightOf="@id/imgSong" />

    <ImageButton
        android:id="@+id/imgViewDetail"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/btn_play"
        android:background="@null"
        android:scaleType="fitXY"
        android:layout_marginRight="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>